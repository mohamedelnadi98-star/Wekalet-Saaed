# متابعة لاعبي وكالة سعيد — الإصدار 10

لوحة `stats.html` تعرض عدد المتصفحات والجلسات، النشطين تقريبًا، وقت اللعب، الأجهزة، آخر مراحل التقدم والأفعال الناجحة وأماكن أخطاء JavaScript. لا تعرض أسماء الزوار أو هوياتهم. البيانات تبدأ بعد الربط؛ لا تسترجع زيارات قديمة.

## حالة الحزمة

واجهة الإحصائيات وخادم الاستقبال موجودان ومختبران محليًا. `analytics-config.js` يحتوي endpoint فارغًا؛ **لم يُنشر خادم ولم يبدأ التجميع**. GitHub Pages يقدم الملفات الثابتة ولا ينفذ `server.mjs`. يلزم خادم Node منفصل مع HTTPS ومساحة دائمة، أو استضافة اللعبة كلها على خادم Node.

## التشغيل والربط

1. جهز خادمًا يدعم Node.js 22 أو أحدث، وانسخ الحزمة العامة إليه. نفذ `npm ci`.
2. أنشئ مفتاح إدارة عشوائيًا من جهازك: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`. احتفظ بالناتج خارج المستودع.
3. اضبط متغيرات البيئة من لوحة استضافة الخادم:
   - `ANALYTICS_ADMIN_KEY`: المفتاح السابق؛ على الأقل 32 حرفًا.
   - `ANALYTICS_ORIGINS`: أصل موقع اللعبة ولوحة المتابعة مفصولين بفواصل، مثل `https://yourname.github.io,https://your-server.example`. الأصل لا يحتوي مسار المستودع أو شرطة أخيرة.
   - `DATA_DIR`: مسار خارج مجلد الملفات العامة على قرص دائم، مثل `/data/saeed`. قاعدة الإحصائيات في `metrics/analytics.json`. احتفظ بنسخة احتياطية للقرص.
   - `HOST=0.0.0.0` و`PORT` حسب منصة الاستضافة. ضع HTTPS أمام الخادم.
4. شغّل `npm start`. في تجربة جهازك يكفي الأصل `http://localhost:8787` وعنوان `http://localhost:8787/api/telemetry`.
5. ضع عنوان الخادم الكامل المنتهي بـ `/api/telemetry` في `endpoint` داخل `analytics-config.js`. **لا تضع مفتاح الإدارة في هذا الملف**.
6. أعد البناء بهذا الترتيب قبل رفع ملفات اللعبة:
   ```sh
   node scripts/build-guide.mjs
   python3 scripts/build-portable.py
   python3 scripts/build-sw.py
   python3 scripts/build-pages.py
   ```
   ملف service-worker يحتوي بصمات الملفات؛ تعديل الإعداد بدون إعادة البناء يجعل التحديث الجديد يفشل في التحقق. ارفع ملفات `_site` للنشر المباشر، أو ملفات المشروع المعتادة عبر GitHub Desktop ليعمل سير النشر الموجود.
7. افتح اللعبة وجرب بدء اللعب، ثم افتح `stats.html` من موقعك. اكتب عنوان الاستقبال ومفتاح الإدارة. يحدث الإرسال كل نحو 8 ثوانٍ والتقرير كل دقيقة. يلزم إبقاء صفحة اللعبة في المقدمة لبعض الوقت لاحتساب وقت اللعب.

## الخصوصية والحدود

- معرّف عشوائي للمتصفح وآخر للجلسة، نوع الجهاز، الإصدار، التقدم، أفعال محددة واسم ملف الخطأ ورقمه فقط. لا تُرسل ملفات الحفظ أو الاسم أو نص رسائل الخطأ.
- عنوان IP يستخدم مؤقتًا في الذاكرة لتحديد معدل الطلبات؛ التطبيق لا يكتبه في قاعدة الإحصائيات. قد تحتفظ منصة الاستضافة بسجلات اتصال مستقلة وفق إعداداتها.
- توجد وسيلة إيقاف المشاركة في إعدادات اللعبة. فشل الشبكة لا يوقف اللعب. نسخة المالك معزولة.
- الاحتفاظ 30 يومًا، وحد أقصى 25 ألف جلسة في هذه النسخة. الحذف الدوري يحدث عند ورود بيانات. لخدمة أكبر يلزم ترحيل التخزين إلى قاعدة بيانات مناسبة.
- العدادات تقديرية؛ تغيير المتصفح يمثّل زائرًا جديدًا، والزيارات المزيفة ممكنة لأن الاستقبال عام. وقت اللعب يُحتسب عندما تكون اللعبة مفتوحة وفي المقدمة وغير متوقفة يدويًا، وقد يشمل قراءة القوائم.
- رسم الأيام يجمع الجلسات بتاريخ بدايتها، والفترة تختار الجلسات التي اتصلت خلالها؛ الجلسات الطويلة قد تبدأ قبل أول يوم في الفترة. آخر خطوة ليست دليلًا على سبب ترك اللاعب للعبة.
- مفتاح الإدارة في ذاكرة لوحة المتابعة فقط، ويُطلب مجددًا بعد إغلاقها. لا يوجد مفتاح مضمّن في النسخة العامة.

مراجع: https://docs.github.com/en/pages/getting-started-with-github-pages/about-github-pages — https://nodejs.org/api/http.html
