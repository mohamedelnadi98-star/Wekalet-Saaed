// Proportional adult meshes replace the previous oversized-head character pack.
export const models={initModels(){},attachActor(){},updateModels(dt,s){for(const g of this.adults||[]){if(!g.parent){this.adults.delete(g);continue;}if(g.userData.seated){for(const leg of g.userData.legs)leg.userData.knee.rotation.x=1.4;continue;}for(const leg of g.userData.legs){leg.userData.knee.rotation.x=Math.max(0,-leg.rotation.x)*.65}for(const arm of g.userData.arms)arm.userData.elbow.rotation.x=(g===this.player&&s.carry||g===this.workerActor&&(s.pickingJob||s.workerJob))?-.7:-.08;g.userData.head.rotation.y=Math.sin(this.elapsed*.4+(g.userData.identity||0))*.045}}};
