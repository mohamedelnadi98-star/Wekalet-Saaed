# تشغيل وكالة سعيد على الإنترنت — الإصدار ٦

اللعب الفردي ما زال يعمل بملف Play-Offline.html أو GitHub Pages. الحساب والحفظ عبر الأجهزة وتبادل الشحنات وتنبيهات الإغلاق تحتاج تشغيل خادم Node المرفق على عنوان HTTPS واحد. رفع الملفات إلى GitHub Pages وحده لا يشغّل الخادم.

## التجربة المحلية

1. ثبّت Node.js 20 أو أحدث وافتح الطرفية داخل مجلد اللعبة.
2. شغّل `node server.mjs` ثم افتح `http://localhost:8787`.
3. من الإعدادات ← الحساب والحفظ عبر الأجهزة، اكتب اسم حساب إنجليزي وكلمة مرور ١٢ حرفًا على الأقل واضغط ربط الوكالة الحالية.
4. اضغط رفع اللعب الحالي للخانة المطلوبة. من متصفح ثانٍ على **نفس العنوان** سجل الدخول واضغط تحديث ثم استرجاع.
5. الدخول الجديد يلغي رمز الجلسة السابق لهذا الحساب؛ لا توجد مشاركة جلسة دائمة على جهازين. حساب منفصل لكل لاعب للتبادل.

الحفظ المحلي تلقائي؛ الرفع للخادم يدوي وواضح. رفض التعارض يمنع رفع نسخة بنفس رقم نسخة قديمة فوق نسخة أحدث. الاسترجاع يعمل نسخة محلية قبل الاستبدال، مع زر تنزيلها. نسخ الصوت محلية منفصلة، ويمكن تصديرها واستيرادها. كلمة المرور تُخزّن باشتقاق scrypt مع salt؛ لا يوجد استرجاع بالبريد أو لوحة إدارة حسابات في هذه النسخة، فاحتفظ بكلمة المرور.

## النشر

انشر المجلد على خادم يدعم Node، مع قرص دائم لمجلد بيانات الخادم. إعدادات البيئة:

- `PORT`: المنفذ، افتراضي 8787.
- `HOST`: افتراضي 127.0.0.1. استخدم 0.0.0.0 داخل حاوية أو منصة تتطلب ذلك.
- `DATA_DIR`: مجلد بيانات دائم خارج الملفات العامة. نسخة احتياطية دورية منه تحفظ الحسابات والشحنات والحفظ.

المرفقات تتضمن Dockerfile وCaddyfile.example. في نشر Docker اربط volume دائمًا على `/data`. شغّل نسخة خادم واحدة فقط على نفس ملفات البيانات؛ التخزين ملف JSON بتحديث ذري وقائمة عمليات متسلسلة داخل العملية، وليس قاعدة بيانات موزعة. واجهة اللعبة وAPI على نفس الأصل؛ الخادم يرفض مصادر الطلبات الخارجية، ولا توجد إعدادات CORS مفتوحة.

ضع النطاق الحقيقي في إعداد Caddy ووجّه DNS للخادم للحصول على HTTPS. لا تنشر مجلد server-data أو ملفات البيئة والمفاتيح. الخادم يقدم أنواع الملفات العامة المحددة فقط ويمنع الوصول إلى server-data. لم يتم نشر أي نطاق أو شراء استضافة ضمن الحزمة.

## تنبيهات بعد إغلاق الصفحة

التنبيهات المضافة للشحنات الواردة من لاعب آخر. اقتصاد اللعب الفردي يظل متوقفًا عند الإغلاق؛ لا توجد طلبات وهمية تتولد بالخلفية.

1. شغّل `npm ci` لتثبيت مكتبة web-push المحددة في package-lock.json.
2. شغّل `node scripts/vapid-keys.mjs` **على خادمك** واحفظ الناتج بسرية. لا ترفعه إلى GitHub.
3. عيّن متغيرات `VAPID_PUBLIC_KEY` و`VAPID_PRIVATE_KEY` و`VAPID_SUBJECT`؛ الأخير بريدك الفعلي بصيغة `mailto:you@example.com`.
4. أعد تشغيل الخادم عبر HTTPS، وافتح اللعبة وسجّل حسابك ثم اضغط تفعيل تنبيهات الشحنات ووافق على إذن المتصفح.
5. اضغط اختبار التنبيه، ثم جرّب إرسال شحنة من حساب آخر بعد إغلاق صفحة المستلم. يجب أن يبقى الخادم شغالًا. إعدادات الجهاز والمتصفح قد تمنع وصول التنبيهات، ولا يمكن ضمان توقيتها.

خدمات Push المدعومة في فحص العنوان: Google FCM وMozilla وApple. لا تُرسل أسماء الحسابات أو كلمات المرور في إشعار؛ يظهر اسم الوكالة المرسل فقط. زر إلغاء التنبيهات يلغي جميع اشتراكات حسابك المحفوظة بالخادم. عند فشل خدمة Push تبقى الشحنة محفوظة بصندوق الوارد وتظهر عند تحديثه.

## التحقق والحدود

اختبارات HTTP تختبر الحسابات والتعارض وخصوصية الحفظ والتبادل وإطلاق طلب التنبيه بمنفذ إرسال بديل. لم يتم اختبار تسليم تنبيه حي إلى هاتف أو نطاق منشور. نظام الاقتصاد على العميل، لذلك التعاون ليس سوقًا تنافسيًا محميًا من تعديل الحفظ. يلزم تجربة أحمال وأمان إضافية قبل إطلاق خدمة عامة كبيرة.
