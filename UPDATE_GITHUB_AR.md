# تحديث وكالة سعيد تلقائيًا — الإصدار 8.1

هذه حزمة كاملة مبنية على v8، وليست ملفات إصلاح منفصلة. لم تُرفع إلى حساب GitHub من هنا.

## إعداد صاحب اللعبة مرة واحدة

1. فك الضغط وارفع **محتويات** مجلد اللعبة إلى جذر المستودع الحالي، حيث index.html. حافظ على نفس المستودع والرابط حتى يظل الحفظ المحلي على عنوانه المعتاد.
2. تأكد من رفع مجلد `.github` بما فيه `.github/workflows/pages.yml`، وكل مجلدات الكود والأصول. لا ترفع node_modules أو server-data أو ملفات كلمات المرور.
3. في GitHub افتح Settings ← Pages ← Build and deployment ← Source واختر **GitHub Actions**.
4. من تبويب Actions شغّل Publish Saeed Agency إذا لم يبدأ تلقائيًا، وانتظر نجاح Deploy باللون الأخضر.
5. بعد ذلك يكفي تعديل الملفات على الفرع الافتراضي ثم Commit. المسار يبني الدليل والحزمة وبصمة الإصدار تلقائيًا ثم ينشر النسخة كاملة. لا تحتاج تغيير رقم الإصدار يدويًا.

إذا كانت ملفات اللعبة داخل مجلد فرعي أو كان للمستودع Workflow نشر آخر، يلزم ضبط مسار العمل قبل تفعيل هذا الملف؛ المثال يفترض أن المشروع موجود في الجذر. إعداد GitHub Actions يتم مرة واحدة بواسطة صاحب الموقع، وليس اللاعبين.

## ما يحدث عند اللاعبين

- عند فتح اللعبة أو الرجوع إليها أو عودة الإنترنت تفحص وجود تحديث، مع فحص إضافي كل دقيقة أثناء ظهور الصفحة. هذا توقيت طلب الفحص؛ التنزيل ونشر GitHub وإدارة المتصفح قد يزيدون المدة.
- تنزيل كل ملفات الإصدار مع تجاوز كاش HTTP، والتحقق من بصمة SHA-256 لكل ملف. لا يُفعّل الإصدار عند وجود ملف ناقص أو مختلط مع إصدار آخر.
- تفعيل العامل الجديد تلقائيًا. الصفحة الحديثة تحفظ التقدم قبل إعادة التحميل، وتستكمل الجلسة إذا كانت مفتوحة.
- النوافذ المفتوحة أو تبادل الشبكة يؤجلان إعادة التحميل حتى تنتهي العملية؛ فشل الحفظ يؤجلها أيضًا. تبويب بالخلفية لا يُعاد تحميله قبل ظهوره.
- لا يُمسح localStorage أو IndexedDB، ولا خانات الحفظ أو تسجيلات الصوت. تُحذف فقط مخابئ إصدارات هذه اللعبة ذات المسار الجديد المحدد.
- دون إنترنت يستمر آخر إصدار مكتمل. الموقع المغلق أو الهاتف غير المتصل لا يمكن ضمان تحديثه لحظة الرفع؛ يلزم عودة الاتصال وفتح اللعبة.
- الجلسات القديمة جدًا التي لم تحتوِ أصلًا على مستمع التحديث قد تظل تعرض الكود المفتوح حتى التنقل أو الفتح الطبيعي التالي. العامل الجديد لا يجبر جلسة قديمة على خسارة تقدمها. لا يلزم مسح بياناتها.

## بديل النشر اليدوي

يمكن رفع الحزمة كاملة بالطريقة السابقة للنشر من فرع، بما فيها service-worker.js الجديد. لكل تعديل كود لاحق يجب تشغيل:

    node scripts/build-guide.mjs
    python3 scripts/build-portable.py
    python3 scripts/build-sw.py

ثم رفع الملفات الناتجة مع بقية الملفات. لهذا يُفضَّل مسار GitHub Actions المرفق، فهو ينفذ البناء تلقائيًا لكل Commit. ملف Play-Offline.html المحلي مستقل، ولن يتحدث عبر الإنترنت؛ التحديث التلقائي خاص برابط الموقع HTTPS.

## التحقق وحدوده

اختبارات آلية تسجيل العامل وإعادة التحميل والحفظ، وفشل البصمة، والعمل دون اتصال، وعزل مخبأ كل لعبة، والحزمة المنشورة. اختبارات التنفيذ تستخدم واجهات متصفح محاكية، ولم يُجرَ اختبار نشر حي على مستودعك أو هاتف فعلي؛ الرابط لم يُرسل بعد.

مصادر السلوك وإعداد النشر:
- https://developer.mozilla.org/en-US/docs/Web/API/ServiceWorkerGlobalScope/skipWaiting
- https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages
